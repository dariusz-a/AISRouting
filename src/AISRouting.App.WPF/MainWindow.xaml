<Window x:Class="AISRouting.App.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AISRouting.App.WPF"
        xmlns:views="clr-namespace:AISRouting.App.WPF.Views"
        mc:Ignorable="d"
        Title="AISRouting" Height="700" Width="1000" MinHeight="700" MinWidth="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Ship Selection View -->
        <views:ShipSelectionView Grid.Row="0" 
                                 DataContext="{Binding ShipSelectionViewModel}"
                                 Margin="0,0,0,10" />

        <!-- Track Results Panel (Placeholder) -->
        <GroupBox Grid.Row="1" Header="Track Results" Margin="0,0,0,10">
            <Grid Margin="10">
                <TextBlock Text="Track results will be displayed here in future features." 
                           FontStyle="Italic" Foreground="Gray" />
            </Grid>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="Ready" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
