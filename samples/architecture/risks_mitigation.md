| Lens | Risk | Impact | Likelihood | Mitigation (Design) | Verification (Test) | Metric/SLO | Owner |
|------|------|---------|-------------|---------------------|---------------------|-------------|--------|
| Failure | Identity Provider (OIDC) outage/latency blocks login/token validation for Shell/Backend API | High | Med | Client/server timeouts (2–5 s) and circuit breaker; cache session/refresh tokens; grace period for existing sessions; Shell status banner | Inject IdP 5xx/timeouts and JWKs unavailability; verify degraded UX, retries with backoff, and existing sessions continue | auth_success_rate ≥ 99%; idp_latency_p95_ms ≤ 300; platform_availability ≥ 99.95% | Backend |
| Failure | Primary Database failover/replication lag causes write errors or stale reads via Backend API | High | Med | Multi‑AZ DB; single‑writer via Backend API; retries with idempotency keys; readiness gates and transaction timeouts | Chaos DB failover; assert RPO ≤ 5 min, RTO ≤ 30 min, replication lag ≤ 2 s; no duplicate writes | db_replication_lag_s ≤ 2; error_rate < 1% | Platform/SRE |
| Failure | Import/ETL Worker processes poison CSV causing retries/DLQ and DB backpressure | Med | Med | Staged validation; idempotency keys; DLQ/quarantine; import parallelism ≤ 2; ingress rate limits and queue‑depth gates | Upload malformed/duplicate CSV; verify DLQ/quarantine, no partial writes, queue depth stays ≤ 2 | import_job_success_rate ≥ 99%; import_queue_depth ≤ 2 | Backend |
| Performance | Backend API p95 latency > 250 ms under 500 RPS burst | High | Med | HPA on CPU 70% and latency p95>250 ms (3 min); HTTP/2 + request batching; short‑lived caches (1–5 s); 429 + Retry‑After | Load test 500 RPS for 5 min; verify autoscale within 2 min, p95 ≤ 300 ms during burst, ≤ 250 ms post‑scale | http_server_request_duration_seconds p95 ≤ 250 ms; error_rate < 1% | Platform/SRE |
| Performance | CDN/Edge Delivery cache miss spikes slow FCP/TTI for Shell/Capability MFEs | Med | Med | Immutable asset hashing; long max‑age; prefetch critical assets; CDN warmers/config checks | Purge CDN and measure; verify cdn_cache_hit_ratio and web‑vitals remain within targets | cdn_cache_hit_ratio ≥ 0.9; web_vitals_fcp_seconds p95 ≤ 1.8 s | Platform/SRE |
| Performance | Import backlog increases job duration beyond 10 s p95 | Med | Med | Cap worker parallelism ≤ 2; queue‑depth gates; throttle uploads; exponential backoff with Retry‑After | Enqueue multiple 5k‑row files; assert queue gating and p95 duration within target | import_job_duration_seconds p95 ≤ 10 s; import_queue_depth ≤ 2 | Backend |
| Evolution | Contract drift between Capability MFEs/typed SDK and Backend API | High | Med | Typed SDK + contract tests; semver; backward‑compatible API; canary per MFE via Feature Flag Service | Break contract in staging; contract tests fail; canary shows elevated 4xx and rolls back | contract_test_pass_rate = 100%; client_4xx_rate < 1% | Backend |
| Evolution | Primary Database schema migrations cause downtime/locking | High | Low | Expand/contract migrations; online backfill; zero‑downtime rollout; tuned DB pools; PDBs | Run online migration under load in staging; measure lock waits and error rates | db_lock_wait_time_ms p95 < 100; migration_error_rate = 0 | Backend |
| Evolution | Upgrading Vue/PrimeVue/Vite or SDK breaks Shell/Capability MFEs | Med | Med | Pin versions and compatibility matrix; CI build + Playwright E2E; feature flags; canary rollout | Run build + E2E (route change + CRUD) before merge; canary deploy verifies metrics | e2e_pass_rate = 100%; spa_route_change_duration_ms p95 ≤ 250 | Frontend |
| Policy | GDPR residency breach (telemetry/backups outside EU region) | High | Low | EU region for Primary Database and Observability/Telemetry Platform; restrict cross‑region endpoints; encryption at rest; config audits | Audit regions for DB/telemetry; synthetic checks target EU endpoints; restore drill validates residency | residency_violations = 0; data_transfer_out_of_region_bytes = 0 | Platform/SRE |
| Policy | RBAC enforcement gaps at Backend API allow unauthorized access | High | Med | Policy/Guard at Adapter/Service entry; JWT claims mapping (OIDC); least‑privilege routes; authz tests | Attempt unauthorized CRUD in tests; expect 403; measure policy coverage | unauthorized_access_rate = 0; authz_test_coverage ≥ 90% | Backend |
| Policy | Audit logs incomplete or include PII beyond policy/retention | Med | Med | Structured logs w/o PII; mark SLO_BREACH; retention ≤ 30 days; user‑initiated purge; sampling controls | Scan logs for PII samples; validate retention config; run purge and verify deletion | PII_fields_in_logs = 0; telemetry_retention_days ≤ 30 | Platform/SRE |